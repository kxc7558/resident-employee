"""resident-employee：给业务系统配常驻 AI 员工的框架。

框架由两半组成，各自解决一个问题：

| 部件 | 管什么 | 状态 |
|---|---|---|
| `sql_guard`（独立仓库） | **能看见什么** —— 数据可见范围 | 已发布，独立安装 |
| `authz` | **能做什么** —— 授权与审计 | 本仓库，v0.1 |

> `sql_guard` 是独立分发的包（`pip install sql-guard`），不在这里重复实现。
> 常驻运行时、网页前台尚未开始。
"""

__version__ = "0.1.0"
